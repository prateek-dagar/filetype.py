from .base import Type as Type

IMAGE: tuple[Type, ...]
VIDEO: tuple[Type, ...]
AUDIO: tuple[Type, ...]
FONT: tuple[Type, ...]
ARCHIVE: tuple[Type, ...]
APPLICATION: tuple[Type, ...]
DOCUMENT: tuple[Type, ...]
TYPES: list[Type]
